import 'package:flutter/material.dart';

class BedStatusWidget extends StatelessWidget {
  const BedStatusWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Beds: 10/20');
  }
}
